---
interface Props {
  title: string;
  description: string;
  href: string;
}

const { title, description, href } = Astro.props;
---

<a class="section-card" href={href}>
  <h3>{title}</h3>
  <p>{description}</p>
</a>
